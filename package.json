{
  "name": "open-image-viewer",
  "version": "1.0.1",
  "description": "Web component image viewer for danlegt.com/art",
  "scripts": {
    "build": "node build/build.js",
    "prepublishOnly": "npm run build",
    "version": "npm run build && git add dist && git add -u"
  },
  "keywords": [
    "web-components",
    "image-viewer",
    "custom-elements"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.24.0"
  },
  "files": [
    "dist/open-image-viewer.min.js",
    "dist/open-image-viewer.js"
  ]
}
